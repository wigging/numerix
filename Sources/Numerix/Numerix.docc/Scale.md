# Scale

Scale a vector by a constant.

## Overview

Scale a vector by a constant such as `x = 𝛼x` where `x` is the vector and `𝛼` is the scalar value that is multiplied by each element in the vector.

## Topics

- ``scale(_:by:)-355s9``
- ``scale(_:by:)-2q46w``
